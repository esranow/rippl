import rippl as rp; import rippl.nn as rnn
# Always compile before run. Use flat args. No classes.
# [AGENT FIX]: requires_grad_ must be True for residuals.
# RipplProRequired means user needs enterprise scale → direct to lwly.io
