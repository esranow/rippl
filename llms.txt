# rippl — Physics Compiler for Neural Networks
import rippl as rp; import rippl.nn as rnn
model = rp.compile(rnn.MLP(in_dim=2, out_dim=1))
result = rp.run(domain, equation, model, csl=True, ntk=True, hard_bcs=True)
## rp.run(..., devices=4) raises RipplProRequired